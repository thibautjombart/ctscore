## Column names referenced via dplyr data-masking (non-standard evaluation).
## Declared here to avoid "no visible binding for global variable" NOTEs in
## R CMD check.
utils::globalVariables(c("contact_id", "date"))
