# Print a `ctdata` object

Print a `ctdata` object

## Usage

``` r
# S3 method for class 'ctdata'
print(x, ...)
```

## Arguments

- x:

  a `ctdata` object

- ...:

  passed to the print methods of the underlying tibbles

## Value

`x`, invisibly.
