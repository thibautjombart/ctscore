% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_ctdata.R
\name{sim_ctdata}
\alias{sim_ctdata}
\title{Simulate contact tracing data}
\usage{
sim_ctdata(
  n_contacts = 100,
  duration = 30,
  incub = 1:7,
  locations = list(default = 1),
  n_exposures = list(default = 1),
  infection_proba = list(default = 0.1),
  type_weights = NULL
)
}
\arguments{
\item{n_contacts}{Number of contacts to simulate.}

\item{duration}{Length of the exposure period (days).}

\item{incub}{Integer vector of incubation periods (days), sampled with replacement.}

\item{locations}{Named list giving the probability of each location being assigned to a contact.
Names must match \code{n_exposures}.}

\item{n_exposures}{Named list giving the number of exposures per contact for each location.
Names must match \code{locations}.}

\item{infection_proba}{Named list giving the probability of infection for each exposure type.
Names must match \code{type_weights}.}

\item{type_weights}{Named list giving the relative frequency of each exposure type. Names must match \code{infection_proba}. Defaults to uniform.}
}
\value{
An object of class \code{c("sim_ctdata", "ctdata", "data.frame")} with one
row per exposure. It carries the standard \code{ctdata} columns (\code{contact_id},
\code{date}, \code{type}, \code{location}, \code{last_visit}, \code{p_infection}) plus the
simulation truth \code{infected} (logical, per contact) and \code{onset} (symptom
onset day, \code{NA} when not infected).
}
\description{
Simulate contact tracing data for a set of contacts. A contact has one or
more exposures, each with a probability of causing infection. Infected
contacts develop symptoms after an incubation period.
}
\examples{
x <- sim_ctdata(
 n_contacts = 10,
duration = 30,
incub = 1:7,
locations = list(cityA = 0.8, cityB = 0.2),
n_exposures = list(cityA = 2, cityB = c(2,2,3,4,5,10)),
infection_proba = list(household = 0.2, funeral = 0.4),
type_weights = list(household = 0.7, funeral = 0.3)
)
head(x)
class(x)
}
\seealso{
\code{\link[=make_ctdata]{make_ctdata()}} to create a \code{ctdata} object from real data.
}
\author{
Cyril Geismar
}
