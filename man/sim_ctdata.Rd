% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_ctdata.R
\name{sim_ctdata}
\alias{sim_ctdata}
\title{Simulate contact tracing data}
\usage{
sim_ctdata(
  n_contacts = 100,
  duration = 30,
  incub = 1:7,
  n_exposures = list(default = 1),
  infection_proba = list(default = 0.1),
  locations = list(default = 1)
)
}
\arguments{
\item{n_contacts}{Number of contacts to simulate.}

\item{duration}{Length of the exposure period (days).}

\item{incub}{Integer vector of incubation periods (days), sampled with
replacement.}

\item{n_exposures}{Named list giving the number of exposures per contact for each location.}

\item{infection_proba}{Named list giving the probability of infection for each exposure type.}

\item{locations}{Named list giving the probability of each location being sampled for a contact.}
}
\value{
An object of class \code{c("sim_ctdata", "ctdata", "data.frame")} with one
row per exposure. It carries the standard \code{ctdata} columns (\code{contact_id},
\code{date}, \code{type}, \code{location}, \code{last_visit}, \code{p_infection}) plus the
simulation truth \code{infected} (logical, per contact) and \code{onset} (symptom
onset day, \code{NA} when not infected).
}
\description{
Simulate contact tracing data for a set of contacts. Each contact has one or
more n_exposures, each with a probability of causing infection. Infected
contacts develop symptoms after an incubation period.
}
\seealso{
\code{\link[=make_ctdata]{make_ctdata()}}, which builds the \code{ctdata} core, and \code{\link[=ctscore]{ctscore()}}.
}
\author{
Cyril Geismar
}
