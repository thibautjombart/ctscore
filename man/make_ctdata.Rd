% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_ctdata.R
\name{make_ctdata}
\alias{make_ctdata}
\title{Build a ctdata object}
\usage{
make_ctdata(exposures, linelist = NULL, infection_proba = list(default = 0))
}
\arguments{
\item{exposures}{a \code{data.frame} of exposures, one row per exposure.
Must contain \code{contact_id}, \code{date} and \code{type}; any extra columns are kept as
exposure-level data. \code{date} may be \code{Date}, numeric, or character (converted
with \code{as.Date}).}

\item{linelist}{an optional \code{data.frame} of individual-level data.
Must contain \code{contact_id} and may contain \code{location}, \code{last_visit}, \code{infected},
\code{onset}, plus any extra columns.
\code{last_visit} and \code{onset} may be \code{Date} or numeric.}

\item{infection_proba}{a named \code{list} giving the probability of infection for
each exposure \code{type}; names must match the types present in \code{exposures}.}
}
\value{
A \code{ctdata} object: a \code{list} of two tibbles sharing a \code{contact_id}
column — \code{linelist} (one row per contact) and \code{exposures} (one row per
exposure, with \code{infection_proba} attached), ordered by contact and date.
}
\description{
Builds a \code{ctdata} object from two tables: \code{exposures} (one row per exposure)
and an optional \code{linelist} (one row per contact). Both are keyed by
\code{contact_id}.
}
\examples{
x <- make_ctdata(
  exposures = tibble::tibble(
    contact_id = c(1, 1, 2, 3),
    date       = Sys.Date() - c(6, 4, 2, 2),
    type       = c("normal", "funeral", "normal", "normal")
  ),
  linelist = tibble::tibble(
    contact_id = c(1, 2, 3),
    location   = "some-town",
    last_visit = Sys.Date() - c(4, 1, NA)
  ),
  infection_proba = list(normal = 0.2, funeral = 0.9)
)
x
class(x)
}
\seealso{
\code{\link[=sim_ctdata]{sim_ctdata()}} to simulate contact tracing data.
}
\author{
Thibaut Jombart / Cyril Geismar
}
