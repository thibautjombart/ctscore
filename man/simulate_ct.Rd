% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_ct.R
\name{simulate_ct}
\alias{simulate_ct}
\title{Simulate contact tracing data}
\usage{
simulate_ct(
  n_contacts = 100,
  duration = 30,
  n_exposures = 1L + rpois(1000L, 1),
  p_inf = list(default = 0.1),
  incub = 1L + rpois(1000L, 7),
  locations = list(default = 1),
  coverage = 0.8,
  followup_delay = 1L + rpois(1000L, 3)
)
}
\arguments{
\item{n_contacts}{Number of contacts to simulate.}

\item{duration}{Length of the exposure period (days). Exposures occur on days
\code{1:duration}.}

\item{n_exposures}{Integer vector giving the number of exposures per contact,
sampled with replacement.}

\item{p_inf}{Named list giving the infection probability for each exposure
type.}

\item{incub}{Integer vector of incubation periods (days), sampled with
replacement.}

\item{locations}{Named list giving the proportion of contacts assigned to
each location. If unnamed, contacts are distributed equally across
locations.}

\item{coverage}{Follow-up probability. Either a single value for all
locations or a named list of location-specific values.}

\item{followup_delay}{Integer vector of delays (days) from exposure to
follow-up, sampled with replacement.}
}
\value{
A \code{sim_ctdata} \code{data.frame} with one row per observed exposure and
columns \code{contact_id}, \code{date}, \code{type}, \code{location}, \code{last_visit},
\code{p_infection}, \code{infected}, \code{date_infection}, and \code{date_onset}.
}
\description{
Simulate contact tracing data for a set of contacts. Each contact has one or
more exposures, each with a probability of causing infection. Infected
contacts develop symptoms after an incubation period and are followed only
while symptom-free. Contacts are assigned to locations with location-specific
follow-up coverage. The output contains one row per observed exposure,
together with the true infection status and dates.
}
\examples{
sim <- simulate_ct(
  n_contacts = 50,
  p_inf = list(household = 0.2, funeral = 0.8)
)
head(sim)

}
\author{
Cyril Geismar
}
