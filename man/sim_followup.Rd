% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_followup.R
\name{sim_followup}
\alias{sim_followup}
\title{Simulate follow-up for contact tracing data}
\usage{
sim_followup(
  x,
  duration = 1,
  coverage = 0,
  strategy = c("random", "geo_random", "ctscore", "geo_ctscore")
)
}
\arguments{
\item{x}{a \code{ctdata} object}

\item{duration}{an \code{integer} indicating the number of days to simulate
follow-up for; starts from the most recent date in \code{x}, be it in follow-up
history or in exposures; defaults to 1 day}

\item{coverage}{the proportion of contacts visited at any time step; defaults
to 0 - no follow-up}

\item{strategy}{a \code{character} indicating the follow-up strategy to use in the
simulations; currently available values are: "random"; see details section
for more information}
}
\description{
This function adds simulated follow-up to a \code{ctdata} object as generated by
\code{\link[=make_ctdata]{make_ctdata()}} or simulated via \code{\link[=sim_ctdata]{sim_ctdata()}}. The function simulates
follow-up for each individual in the \code{ctdata} object, based on the specified
follow-up time and the probability of being followed up. The function returns
a modified \code{ctdata} object with the simulated follow-up data added.
}
\details{
Available follow-up strategies (\code{strategy} argument) include:
\itemize{
\item "random": individuals are visited at random every day of the simulation
\item "geo_random": locations are prioritized at random every day of the
simulation; as many contacts as possible are visited in the first chosen
location, then if capacity remains, in the second, third, etc.
\item "ctscore": individuals are prioritized by highest ctscore every day of the
simulation
\item "geo_ctscore": geographic locations are prioritized according by highest
ctscore every day of the simulation; as many contacts as possible are
visited in the first chosen location, then if capacity remains, in the
second, third, etc.
}
}
\seealso{
\code{\link[=make_ctdata]{make_ctdata()}} to create contact tracing data from existing data,
or \code{\link[=sim_ctdata]{sim_ctdata()}} to simulate them
}
\author{
Thibaut Jombart
}
