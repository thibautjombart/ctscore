% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctdata.R
\name{as_tibble.ctdata}
\alias{as_tibble.ctdata}
\title{Coerce a \code{ctdata} object to a tibble}
\usage{
\method{as_tibble}{ctdata}(x, ..., by_contact = FALSE)
}
\arguments{
\item{x}{a \code{ctdata} object}

\item{...}{ignored}

\item{by_contact}{\code{logical}; return one row per contact with nested
exposures (\code{TRUE}), or one row per exposure (\code{FALSE}, default)}
}
\value{
A tibble.
}
\description{
Two formats are available:
\itemize{
\item \code{by_contact = FALSE} (default): one row per exposure, with contact-level
\code{linelist} columns added to each row.
\item \code{by_contact = TRUE}: one row per contact, with exposures stored in a nested
\code{exposures} list-column containing \code{date}, \code{type}, and \code{infection_proba}.
}
}
\examples{
x <- sim_ctdata(5)
as_tibble(x) # flat: one row per exposure
as_tibble(x, by_contact = TRUE) # nested: one row per contact
}
